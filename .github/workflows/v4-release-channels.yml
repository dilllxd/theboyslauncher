name: V4 Release Channels

on:
  workflow_dispatch:
    inputs:
      channel:
        description: "Release channel to build"
        required: true
        default: dev
        type: choice
        options:
          - dev
          - stable
  push:
    branches:
      - main
      - dev
    paths:
      - ".github/workflows/v4-release-channels.yml"
      - "Cargo.toml"
      - "Cargo.lock"
      - "crates/**"
      - "src-tauri/**"
      - "frontend/**"
      - "package.json"
      - "package-lock.json"
      - "scripts/clean-tauri-updater-artifacts.mjs"
      - "scripts/configure-tauri-release-channel.mjs"
      - "scripts/generate-tauri-updater-manifest.mjs"
      - "scripts/preflight-v4-release.mjs"
      - "scripts/resolve-v4-release-version.mjs"
      - "scripts/smoke-packaged-exe.mjs"
      - "scripts/stage-social-backend.mjs"
      - "scripts/test-release-channel-scripts.mjs"
      - "scripts/verify-package-resources.mjs"
      - "scripts/verify-tauri-bundles.mjs"
      - "scripts/verify-tauri-security-config.mjs"

permissions:
  contents: write

env:
  CARGO_INCREMENTAL: 0
  CARGO_TERM_COLOR: always
  RUSTC_WRAPPER: sccache
  SCCACHE_GHA_ENABLED: "true"

concurrency:
  group: v4-release-${{ github.event_name == 'workflow_dispatch' && inputs.channel || github.ref_name }}
  cancel-in-progress: ${{ github.event_name == 'push' && github.ref_name == 'dev' }}

jobs:
  resolve:
    name: Resolve release version
    runs-on: windows-latest
    timeout-minutes: 10
    if: github.event_name == 'workflow_dispatch' || github.ref_name == 'main' || github.ref_name == 'dev'
    outputs:
      channel: ${{ steps.version.outputs.channel }}
      version: ${{ steps.version.outputs.version }}
      tag: ${{ steps.version.outputs.tag }}
      manifest_name: ${{ steps.version.outputs.manifest_name }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          fetch-tags: true

      - name: Resolve release channel
        id: channel
        shell: bash
        run: |
          if [[ "${GITHUB_EVENT_NAME}" == "workflow_dispatch" ]]; then
            channel="${{ inputs.channel }}"
          elif [[ "${GITHUB_REF_NAME}" == "main" ]]; then
            channel="stable"
          elif [[ "${GITHUB_REF_NAME}" == "dev" ]]; then
            channel="dev"
          else
            echo "::error::Unsupported release ref ${GITHUB_REF_NAME}"
            exit 1
          fi
          echo "channel=${channel}" >> "${GITHUB_OUTPUT}"

      - name: Verify official release repository
        shell: pwsh
        run: |
          if ($env:GITHUB_REPOSITORY -ne "dilllxd/theboyslauncher") {
            Write-Error "Release packaging is only allowed from dilllxd/theboyslauncher; current repository is $env:GITHUB_REPOSITORY."
            exit 1
          }

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm

      - name: Install npm dependencies
        run: npm ci

      - name: Resolve next version
        id: version
        run: npm run resolve:v4-release-version -- ${{ steps.channel.outputs.channel }}

  verify:
    name: Verify release inputs
    runs-on: windows-latest
    timeout-minutes: 25
    needs: resolve

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Set up sccache
        uses: mozilla-actions/sccache-action@v0.0.9

      - name: Cache Cargo
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: |
            . -> target

      - name: Install npm dependencies
        run: npm ci

      - name: Verify release channel scripts
        run: npm run test:release-channels

      - name: Check Rust formatting
        run: cargo fmt --all --check

      - name: Check Rust workspace
        run: cargo check --workspace

      - name: Verify Tauri security config
        run: npm run verify:tauri-security

  test-launcher-core:
    name: Test launcher-core
    runs-on: windows-latest
    timeout-minutes: 20
    needs: resolve

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Set up sccache
        uses: mozilla-actions/sccache-action@v0.0.9

      - name: Cache Cargo
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: |
            . -> target

      - name: Run launcher-core tests
        run: cargo test -p launcher-core --lib -- --test-threads=1

  test-social-backend:
    name: Test social-backend
    runs-on: windows-latest
    timeout-minutes: 15
    needs: resolve

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Set up sccache
        uses: mozilla-actions/sccache-action@v0.0.9

      - name: Cache Cargo
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: |
            . -> target

      - name: Run social-backend tests
        run: cargo test -p social-backend --lib -- --test-threads=1

  package:
    name: Build signed ${{ needs.resolve.outputs.channel }} bundle
    runs-on: windows-latest
    timeout-minutes: 60
    needs: resolve
    if: github.event_name == 'workflow_dispatch'

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          fetch-tags: true

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Set up sccache
        uses: mozilla-actions/sccache-action@v0.0.9

      - name: Cache Cargo
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: |
            . -> target

      - name: Install npm dependencies
        run: npm ci

      - name: Configure Tauri release channel
        run: npm run configure:tauri-release-channel -- ${{ needs.resolve.outputs.channel }} ${{ needs.resolve.outputs.version }}

      - name: Build signed Tauri bundles
        run: npm run tauri:build
        env:
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
          THEBOYS_RELEASE_TAG: ${{ needs.resolve.outputs.channel == 'dev' && 'dev-latest' || needs.resolve.outputs.tag }}
          THEBOYS_UPDATER_MANIFEST_NAME: ${{ needs.resolve.outputs.manifest_name }}
          THEBOYS_CURSEFORGE_API_KEY: ${{ secrets.THEBOYS_CURSEFORGE_API_KEY }}
          VITE_THEBOYS_RELEASE_CHANNEL: ${{ needs.resolve.outputs.channel }}

      - name: Smoke packaged executable
        run: npm run smoke:packaged-exe -- --wait-ms 60000

      - name: Verify Windows bundles
        run: npm run verify:tauri-bundles
        env:
          THEBOYS_UPDATER_MANIFEST_NAME: ${{ needs.resolve.outputs.manifest_name }}

      - name: Upload Windows installer artifacts
        uses: actions/upload-artifact@v4
        with:
          name: TheBoysLauncher-${{ needs.resolve.outputs.channel }}-${{ needs.resolve.outputs.version }}-${{ github.sha }}
          if-no-files-found: error
          retention-days: 14
          path: |
            target/release/bundle/msi/*.msi
            target/release/bundle/nsis/*setup.exe
            target/release/bundle/**/*.sig
            target/release/bundle/latest*.json

  publish:
    name: Publish ${{ needs.resolve.outputs.channel }} release
    runs-on: windows-latest
    timeout-minutes: 20
    needs:
      - resolve
      - verify
      - test-launcher-core
      - test-social-backend
      - package
    if: github.event_name == 'workflow_dispatch'

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          fetch-tags: true

      - name: Download Windows installer artifacts
        uses: actions/download-artifact@v4
        with:
          name: TheBoysLauncher-${{ needs.resolve.outputs.channel }}-${{ needs.resolve.outputs.version }}-${{ github.sha }}
          path: target/release/bundle

      - name: Create immutable version tag
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          if git rev-parse "${{ needs.resolve.outputs.tag }}" >/dev/null 2>&1; then
            existing_sha="$(git rev-list -n 1 "${{ needs.resolve.outputs.tag }}")"
            if [[ "${existing_sha}" != "${GITHUB_SHA}" ]]; then
              echo "::error::Tag ${{ needs.resolve.outputs.tag }} already exists at ${existing_sha}, not ${GITHUB_SHA}."
              exit 1
            fi
            echo "Tag ${{ needs.resolve.outputs.tag }} already points at ${GITHUB_SHA}; continuing publish retry."
            exit 0
          fi
          git tag "${{ needs.resolve.outputs.tag }}" "${GITHUB_SHA}"
          git push origin "${{ needs.resolve.outputs.tag }}"

      - name: Publish stable release
        if: needs.resolve.outputs.channel == 'stable'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ needs.resolve.outputs.tag }}
          name: TheBoysLauncher ${{ needs.resolve.outputs.tag }}
          prerelease: false
          draft: false
          generate_release_notes: true
          files: |
            target/release/bundle/**/*.msi
            target/release/bundle/**/*setup.exe
            target/release/bundle/**/*.sig
            target/release/bundle/**/latest.json

      - name: Refresh dev release
        if: needs.resolve.outputs.channel == 'dev'
        shell: pwsh
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          $releaseTag = "dev-latest"
          gh release view $releaseTag *> $null
          if ($LASTEXITCODE -ne 0) {
            gh release create $releaseTag --target "$env:GITHUB_SHA" --title "TheBoysLauncher Dev" --prerelease --notes "Latest development channel build."
          } else {
            gh release edit $releaseTag --target "$env:GITHUB_SHA" --title "TheBoysLauncher Dev" --prerelease --notes "Latest development channel build."
          }

          $assetPatterns = @(
            "target/release/bundle/**/*.msi",
            "target/release/bundle/**/*setup.exe",
            "target/release/bundle/**/*.sig",
            "target/release/bundle/**/latest-dev.json"
          )
          $assets = @()
          foreach ($pattern in $assetPatterns) {
            $assets += Get-ChildItem -Path $pattern -File
          }
          if ($assets.Count -eq 0) {
            Write-Error "No release assets were found."
            exit 1
          }
          gh release upload $releaseTag @($assets.FullName) --clobber
